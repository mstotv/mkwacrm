# نظرة عامة ودليل نظام WaCRM (MitaKurd WhatsApp Auto)

هذا الملف هو المرجع الأساسي والشامل لمنصة **WaCRM**؛ حيث يوثق البنية البرمجية، وقواعد البيانات، ونظام الترجمة، وتنسيق الألوان، والصفحات الأساسية. قراءة هذا الملف تغنيك عن تحليل جميع ملفات المشروع عند البدء بأي عمل جديد.

---

## 1. نظرة عامة على المنصة (Platform Overview)
المنصة عبارة عن نظام **إدارة علاقات عملاء (CRM) متعدد المشتركين (SaaS)** مخصص لواتساب، يتيح للشركات والفرق:
* استقبال وإرسال رسائل واتساب عبر صندوق وارد مشترك (Shared Inbox).
* تصنيف جهات الاتصال وتقسيمهم (Contacts Segmentation) ومزامنتها مع Google Sheets.
* إدارة مراحل المبيعات والعملاء (Sales Pipelines).
* إطلاق حملات البث الجماعي (Broadcasts) المجدولة والتلقائية.
* بناء ردود تلقائية ذكية باستخدام الذكاء الاصطناعي (OpenAI & DeepSeek) وبوتات الكلمات المفتاحية (Keyword Bots).
* إنشاء مسارات تفاعلية متقدمة (Flows) بدون كود.

---

## 2. التقنيات المستخدمة (Technology Stack)
* **الموقع والواجهة الخلفية**: Next.js 16.2.6 (App Router) مع React 19.
* **قاعدة البيانات والخدمات السحابية**: Supabase (Database, Auth, Storage, Realtime).
* **التنسيق والتصميم**: Tailwind CSS v4 مع Vanilla CSS وتنسيقات مخصصة (Custom Themes).
* **المكتبات الرسومية وتكامل البيانات**:
  * `@xyflow/react` لبناء مخططات الأتمتة والمسارات.
  * `@dnd-kit/core` لسحب وإفلات العناصر (Drag & Drop).
  * `lucide-react` للأيقونات.
  * `sonner` للتنبيهات (Toasts).
  * `recharts` للرسوم البيانية والإحصائيات.

---

## 3. البنية البرمجية وهيكل المجلدات (Folder Structure)

```
wacrm/
├── src/
│   ├── app/                 # مسارات وتوجيهات Next.js (App Router) والـ APIs
│   │   ├── (auth)/          # صفحات تسجيل الدخول، التسجيل، واستعادة الحساب
│   │   ├── (dashboard)/     # صفحات لوحة تحكم المشتركين
│   │   ├── admin/           # لوحة الإدارة العليا (Super Admin Dashboard)
│   │   ├── api/             # واجهات البرمجة الخلفية (API Routes)
│   │   ├── layout.tsx       # التخطيط الرئيسي وجلب الإعدادات والسمات
│   │   └── page.tsx         # الصفحة الرئيسية (Landing Page)
│   ├── components/          # المكونات البرمجية للواجهة مقسمة حسب الميزة
│   │   ├── auth/            # مكونات التحقق والصلاحيات
│   │   ├── automations/     # مكونات بناء الأتمتة
│   │   ├── broadcasts/      # مكونات البث المباشر
│   │   ├── contacts/        # مكونات إدارة وتصفية جهات الاتصال
│   │   ├── dashboard/       # مكونات إحصائيات لوحة التحكم
│   │   ├── flows/           # مكونات بناء المسارات (Flow Builder)
│   │   ├── inbox/           # صندوق الوارد المشترك
│   │   ├── landing/         # مكونات الصفحة الهبوطية (Landing Page)
│   │   ├── layout/          # الشريط الجانبي (Sidebar) والرأس (Header)
│   │   ├── settings/        # لوحات الإعدادات (AI, WhatsApp, Billing)
│   │   └── ui/              # مكونات واجهة المستخدم الأساسية (Shadcn/UI)
│   ├── hooks/               # الخطافات المخصصة (Custom Hooks)
│   │   ├── use-auth.tsx     # إدارة حالة المستخدم والصلاحيات والمشتركين
│   │   ├── use-theme.tsx    # إدارة مظهر الواجهة (Dark/Light & Themes)
│   │   ├── use-language.tsx # نظام الترجمة ثنائي اللغة (AR/EN)
│   │   └── use-site-settings.tsx # جلب إعدادات المنصة المخصصة من قاعدة البيانات
│   ├── lib/                 # الدوال المساعدة وإعدادات الاتصال
│   │   ├── supabase/        # اتصالات العميل والخادم بقاعدة بيانات Supabase
│   │   ├── themes.ts        # مصفوفة الألوان والسمات المدعومة
│   │   └── whatsapp/        # منطق الاتصال بـ WhatsApp API ومعالجة الرسائل
│   ├── locales/             # ملفات الترجمة الثابتة (ar.json, en.json)
│   └── types/               # تعريفات الأنواع (TypeScript Types)
├── supabase/
│   └── migrations/          # ملفات الترحيل الهيكلية لقاعدة البيانات (Migrations)
└── package.json             # الاعتماديات والبرامج النصية للمشروع
```

---

## 4. بنية قاعدة البيانات (Database Schema Overview)

تدار الجداول والصلاحيات عبر Supabase RLS (Row Level Security)، وتتكون البنية من جداول رئيسية أهمها:
1. **`accounts`**: يمثل الحسابات أو مساحات العمل المشتركة (Tenants).
2. **`profiles`**: يمثل المستخدمين وصلاحياتهم داخل المنصة ورتبهم الإدارية (`platform_role`).
3. **`account_members`**: يحدد علاقة الأعضاء بالحسابات والأدوار الممنوحة لهم (`owner`, `admin`, `agent`, `viewer`).
4. **`invitations`**: إدارة دعوة الموظفين للانضمام إلى مساحة العمل.
5. **`contacts`**: تخزين بيانات العملاء وجهات الاتصال، ومستوى نقاطهم، ولون التصنيف وعنوانهم.
6. **`pipelines` و `pipeline_stages` و `deals`**: إدارة مراحل المبيعات والصفقات.
7. **`broadcasts` و `broadcast_recipients`**: إدارة البث الجماعي وسجلات الاستلام والـ wamid الخاص بالرسائل.
8. **`automations`**: لتخزين بوتات الرد الآلي وقواعد الأتمتة المخصصة.
9. **`flows`**: مسارات تدفق الرسائل التفاعلية (Interactive Flow Chart JSON).
10. **`tickets` و `ticket_messages`**: نظام تذاكر الدعم الفني بين المشترك والإدارة العليا.
11. **`site_settings`**: الإعدادات العامة للمنصة مثل الاسم، الشعار، والألوان الرئيسية.
12. **`landing_page_settings`**: إعدادات الصفحة الهبوطية والأسعار والخطط وقوالب المميزات.

---

## 5. نظام اللغات والترجمة (Bilingual System - i18n)
* **المسؤول**: الخطاف المخصص `useLanguage` والسياق `LanguageProvider` في المجلد `src/hooks/use-language.tsx`.
* **الملفات**: `ar.json` (العربية) و `en.json` (الإنجليزية) تحت `src/locales/`.
* **طريقة العمل**:
  * يحفظ الاختيار في `localStorage` بمفتاح `app_language`.
  * يتم تطبيق `lang="ar"` أو `lang="en"` وتحديث الاتجاه `dir="rtl"` أو `dir="ltr"` ديناميكياً على عنصر `<html>`.
  * تستخدم دالة الترجمة `t('key.subkey')` لقراءة المفاتيح المتداخلة.
* **صفحة الهبوط (Landing Page)**:
  * تمت ترقيتها لترتبط مباشرة بـ `useLanguage` لتكون متزامنة تماماً مع النظام، مما يتيح التبديل الفوري وتعديل الاتجاه وسلاسة الانتقال عند تسجيل الدخول.

---

## 6. نظام المظهر والسمات (Theme & Color Modes)
* **المسؤول**: الخطاف `useTheme` والسياق `ThemeProvider` في المجلد `src/hooks/use-theme.tsx`.
* **طريقة العمل**:
  * يتم تخزين وضع المظهر (Dark / Light) في `localStorage` بمفتاح `wacrm.colorMode`.
  * يُطبق المظهر على `<html>` بإضافة الكلاس المناسب (`dark` أو `light`) والخاصية `data-mode`.
  * تعتمد مكونات لوحة التحكم بالكامل على نظام Tailwind الداكن/الفاتح.
  * **صفحة الهبوط (Landing Page)**: أصبحت تدعم الوضعين (Dark & Light) باحترافية، حيث يتم تبديل المتغيرات اللونية الخاصة بها (`--ld-bg`, `--ld-text`, `--ld-border`, الخ) تلقائياً عند تغيير الكلاس المطبق على `<html>` وتحديث الأيقونات وعناصر التنقل بشكل تفاعلي.

---

## 7. الدليل السريع للمطورين (Quick Development Guide)

### لإضافة مفتاح ترجمة جديد:
1. افتح `src/locales/en.json` وأضف المفتاح باللغة الإنجليزية.
2. افتح `src/locales/ar.json` وأضف نفس المفتاح باللغة العربية متطابقاً تماماً مع الهيكل الإنجليزي.
3. استخدمه في المكونات باستدعاء:
   ```tsx
   const { t } = useLanguage();
   // الاستخدام:
   t('common.save')
   ```

### لتفعيل ميزات الداكن/فاتح في صفحة الهبوط:
* لا تستخدم ألواناً صلبة مثل `color: #0f172a` أو الخلفية البيضاء الثابتة.
* استخدم المتغيرات المخصصة لصفحة الهبوط مثل:
  * الخلفية: `var(--ld-bg)`
  * خلفية مخففة: `var(--ld-bg-subtle)`
  * البطاقات: `var(--ld-card)`
  * لون النصوص الرئيسي: `var(--ld-text)`
  * لون النصوص المساعد: `var(--ld-text-muted)`
  * الحدود والإطارات: `var(--ld-border)`

---

## 8. سجل الإصلاحات البرمجية والميزات الإضافية (Fixes & Features Log)

تم توثيق وحل المشاكل البرمجية وإضافة الميزات التالية للحفاظ على استقرار النظام وسرعة البناء (Build) وتجربة المستخدم:

### أ. إصلاحات لوحة تحكم الإدارة (`src/app/admin/page.tsx`):
* **مشكلة التجزئة JSX**: تسبب نسيان إغلاق الوسم `<AdminWrapper>` في خطأ في البناء. تم حلها بإضافة الوسم المغلق `</AdminWrapper>` في نهاية حاوية العرض.
* **دوال غير معرّفة**: كانت دالة `createClient` مستخدمة دون استيرادها. تم حلها بإضافة استيراد `createClient` من مكتبة Supabase للعميل.

### ب. إصلاحات حاوية حماية الإدارة (`src/components/admin/admin-wrapper.tsx`):
* **خطأ علامات الاقتباس (Unterminated String)**: السطر الأول كان يحتوي على `"use client';` (خلط بين علامة اقتباس مزدوجة وفردية). تم تعديلها لتصبح `'use client';`.
* **مطابقة أنواع TypeScript**: واجه المترجم مشكلة عند إسناد بيانات المستخدم إلى الحالة الافتراضية `useState(null)`. تم تعريفها كـ `useState<any>(null)` لمنح المرونة وتخطي مشكلة التعيين.

### ج. ميزة شريط العلامات التجارية المتكرر (Infinite Logo Marquee):
* تم بناء مصفوفة تحتوي على أسماء الجهات والعملات الأجنبية المطلوبة (`Meta, Evolution API, WhatsApp, Supabase, ChatGPT, DeepSeek, Google, Google Sheets, Google Calendar, Plisio, Bitcoin, USDT`) لتمريرها ديناميكياً.
* تصميم حركة دوران لا نهائية بالـ CSS تلتزم بالاتجاه الصحيح للنص بناءً على لغة الصفحة (RTL تدور لليمين، و LTR تدور لليسار) مع تأثير تدرج لوني ناعم يتلاشى عند حواف المكون ليتوافق مع الخلفية في الوضعين الفاتح والداكن.

### د. نظام دورات الفوترة المرنة والتجارب المجانية (Flexible Billing Cycles & Free Trials):
* **توسيع البنية الهيكلية:** تم استبدال خيارات الفوترة الثنائية (شهري/سنوي) بمصفوفة خيارات الفوترة `billing_options` في جدول `subscription_plans` لدعم خيارات متعددة مرنة: عدد أيام مخصص، شهري، سنوي، ومدى الحياة (Lifetime) دفعة واحدة بدون تجديد أو تاريخ انتهاء صلاحية.
* **فترات التجربة المجانية (Free Trials):** تفعيل حقل `trial_period_days` في الخطط ومزامنة الرصيد الاستهلاكي للتجربة بحيث يمنع النظام تكرار الحصول عليها للحساب الواحد.
* **واجهات التفعيل والدفع:** تعديل لوحة المسؤول (Admin Panel) لإضافة التفعيل اليدوي متعدد الدورات، وربط بوابات دفع Plisio بآلية الفوترة والاحتساب الجديدة.
* **شريط التنبيه الترويجي:** تم وضع شريط ترويجي وتنبيهي مميز في الشريط الجانبي للمشتركين الذين يخوضون فترات تجريبية يعلمهم بالأيام المتبقية ويوجههم لتنشيط الحساب والدفع.

### هـ. مسار الأسئلة والأجوبة المبرمج (Scripted Q&A Flow / User Input Flow):
* **نوع خطوة مخصص:** إضافة إجراء جديد للسيناريوهات باسم `question_sequence` يسمح للمستخدم بكتابة تسلسل أسئلة يطرحها البوت على العميل خطوة بخطوة وحفظ الردود في متغيرات مستقلة بالذاكرة التخزينية.
* **جدول تتبع الجلسات:** استخدام جدول `automation_qna_sessions` لتسجيل حالة العميل المعلقة، رقم السؤال الحالي، الإجابات المجمعة، وموضع الخطوة القادمة لاستئناف العمل دون انقطاع.
* **اعتراض رسائل العملاء (Webhooks):** دمج دالة المعالجة `handleQnaSessionResponse` في ويب هوكس Meta و Evolution لتمرير الرسائل المستلمة كإجابة فورية وتجميد المحركات المتعارضة (AI/Keywords).
* **الاستئناف التلقائي:** فور تجميع آخر إجابة، تستأنف الأتمتة عملها من النقطة المخزنة (مثل خطوة ترحيل البيانات إلى Google Sheets التي تطابق المتغيرات وتحفظها كصف كامل).
* **محرر الواجهة المرئي:** إضافة محرك تعديل كامل الخطوات في الـ Workflow Builder يتيح إدراج الأسئلة وتسمية حقول المتغيرات وتحديد نوع الإدخال (نص، رقم، ميديا).

### ز. إصلاح نظام الفوترة والاشتراكات اليدوية وتفعيل التجربة المجانية (Subscription RLS & Admin Manual Upgrades Hotfix):
* **مشكلة قراءة الاشتراكات (RLS Select Block):** تم حل مشكلة ظهور الباقات المرقّاة كـ "Free / Inactive" للمستخدمين بسبب سياسات RLS على جدول `account_subscriptions` التي تمنع الـ Client-side query. تم إنشاء مسار API خلفي آمن `/api/billing/subscription` يستعلم بـ `service_role` ويتحقق من JWT لجلسة العميل لخدمة شريط التنقل والفوترة وإعدادات الذكاء الاصطناعي وصوت نجاح الدفع [success/page.tsx](file:///c:/Users/Mustafa/Desktop/wacrm/src/app/billing/success/page.tsx).
* **مشكلة الترقية اليدوية (RLS Write Block):** تم إصلاح شاشات الترقية اليدوية بالأدمن بانل (في صفحة الحسابات وصفحة الاشتراكات) لتتم الكتابة ويسجل تاريخ الدفع `payment_history` عبر طلبات API آمنة موحدة بدلاً من كتابة Supabase مباشرة من المتصفح التي كانت تفشل بصمت بسبب RLS.
* **مشكلة تفعيل الفترة التجريبية المجانية (Trial RLS Block):** تم تعديل مسار تفعيل التجربة المجانية `/api/billing/trial/activate` ليكتب التحديثات في جدول الاشتراكات باستخدام الـ `service_role` بعد التحقق من أحقية المالك والـ JWT بدلاً من طلبات الـ client-side التي كانت تفشل بصمت.
* **أداة الفحص التشخيصي:** تم توفير سكريبت اختبار وصلاحيات [diagnose_subs.js](file:///c:/Users/Mustafa/Desktop/wacrm/diagnose_subs.js) لمحاكاة دخول المستخدمين وفحص استجابة RLS.
* **إصلاح تحذيرات CSS الكاذبة:** تم إنشاء إعدادات لمنع التحذيرات الكاذبة لـ Tailwind CSS v4 في VS Code عبر `.vscode/settings.json`.

### ح. تحديثات وإصلاحات اليوم (Localization, UI responsive upgrade, AI & Integrations Consolidation & Follow-ups):
1. **ميزة المتابعة التلقائية (Follow-up System)**:
   * **جدول follow_ups:** إنشاء جدول المتابعات لتخزين المتابعات المجدولة تلقائياً عندما يطلب العميل تأجيل قرار أو يحدد موعداً لاحقاً.
   * **أداة الذكاء الاصطناعي:** بناء وتكامل أداة `schedule_follow_up` مع مساعد الذكاء الاصطناعي (AI Agent) وجدولتها بالوقت والتاريخ الفعليين.
   * **مهمة كرون الدورية:** تهيئة تشغيل دوري (Cron Job) للتحقق وإرسال تنبيهات المتابعة للمسؤول أو إخطار العميل لمنع تكرار المتابعات واكتشاف نية التأجيل بكفاءة.
2. **توطين وتعريب المنصة بالكامل (Comprehensive Arabic & English Localization)**:
   * **إصلاح شاشة العملاء:** حل مشكلة فقدان كلمة `return` في ملف [contacts/page.tsx](file:///c:/Users/Mustafa/Desktop/wacrm/src/app/(dashboard)/contacts/page.tsx) مما أعاد استقرار بناء المشروع بنجاح.
   * **تعريب إحصائيات لوحة التحكم:** تعريب العناوين، بطاقات الأداء، ومخططات المحادثات بمرور الوقت، الصفقات، ومتوسط وقت الاستجابة مع الأيام والتلميحات بالكامل باللغة العربية.
   * **تعريب جهات الاتصال والصفقات:** ترجمة الحقول المخصصة، شريط البحث، جدول جهات الاتصال، تهيئة مراحل المبيعات الافتراضية بالعربية (عميل محتمل، عميل مؤهل، تم تقديم العرض، مفاوضات، صفقة ناجحة)، وجميع أزرار كانبان.
   * **تعريب الأتمتة وحملات البث:** تعريب شاشات الفراغ وجداول حملات البث بالكامل، إضافة إلى تعريب قوالب البدء السريع الأربعة للأتمتة (ترحيبية، خارج العمل، تأهيل، تذكير) ومشغل الأتمتة وأوقات المتابعة.
   * **تعريب المسارات التفاعلية (Flows Beta):** تعريب العنوان ووصف أزرار المحادثة، قالب "قائمة ترحيبية"، "بوت الأسئلة"، "توجيه المحادثات"، وعدد عقد كل قالب.
3. **تطوير توافق الجوال ودمج التكاملات في شاشة الإعدادات**:
   * **حل مشكلة الجوال:** تحويل قائمة تبويبات صفحة الإعدادات الطويلة إلى قائمة منسدلة (Dropdown Select) تظهر تلقائياً على شاشات الهواتف لمنع تداخلها.
   * **دمج التكاملات والربط:** تجميع "Google Sheets" و "Google Calendar" و "Telegram Notifications" وإعدادات "الذكاء الاصطناعي (AI Settings)" في علامات تبويب فرعية أنيقة تحت تبويب رئيسي واحد باسم **"التكاملات والربط" (Integrations)**.
4. **نظام إدارة صفحات الموقع والحماية القانونية (Site Pages CMS & Legal Auth)**:
   * **نظام إدارة محتوى الصفحات (CMS):** تم إضافة واجهة إدارة مدمجة في صفحة (Admin Settings > Landing Page) تمكّن الأدمن من تعديل محتوى صفحات (Privacy Policy, Terms of Service, About Us, Contact) باللغتين العربية والإنجليزية. وتم بناء صفحة عرض ديناميكية لها على المسار `/p/[slug]`.
   * **الحماية القانونية الإلزامية:** إضافة حقلي اختيار (Checkboxes) إجبارية للموافقة على شروط الخدمة وسياسة الخصوصية في صفحة التسجيل `/signup`. تم ربطها برمجياً لتعطيل زر "التسجيل عبر تليجرام" وزر "إنشاء حساب" العادي لحين تفعيلهما لحماية المنصة.
   * **تعريب صفحات التحقق (Auth Localization):** تم تعريب وتوطين جميع نصوص صفحتي تسجيل الدخول والتسجيل لتتفاعل تلقائياً مع لغة العميل (ar/en) المحددة من خلال `useLanguage()`.

### ط. هندسة الاشتراكات الديناميكية وتطويرات لوحة التحكم (Dynamic Subscriptions & Admin UI Enhancements):
1. **هندسة ميزات الاشتراكات (Feature Library Architecture):**
   * تم استبدال تخزين مميزات الخطط كنصوص عادية (JSONB) بنظام قواعد بيانات علائقي متكامل (Normalized Database).
   * إنشاء جدول `plan_features_library` لتسجيل الميزات الأساسية (عربي/إنجليزي).
   * إنشاء جدول `plan_feature_assignments` لتخصيص الميزات لكل خطة وتحديد حدود الاستخدام (Usage Limits)، حدود البث (Bulk Limits)، وخيار العرض في صفحة الهبوط (Show on Landing).
2. **ديناميكية صفحة الهبوط والفوترة (Dynamic Landing & Billing):**
   * ربط مسار واجهة برمجة التطبيقات `/api/public/plans` لجلب الخطط وميزاتها المفعلة بشكل ديناميكي ومباشر.
   * أصبحت جداول الأسعار في صفحة الهبوط (Landing Page) ولوحة فوترة المستخدم (User Billing Panel) تقرأ وتحدث بيانات الميزات وحدودها (مثل "غير محدود" أو أرقام معينة) تلقائياً عند تغييرها من قبل المسؤول.
3. **تصميم واجهات SaaS احترافية (Premium Admin UI):**
   * إعادة تصميم صفحة الاشتراكات في لوحة التحكم الإدارية لتصبح واجهة (Premium SaaS) مع تأثيرات زجاجية (Glassmorphism)، تدرجات لونية، ومحاذاة أنيقة وتحديد ألوان مميزة لخطط الـ PRO.
   * تحسين بطاقات الخطط لتكون قابلة للتجاوب ومناسبة لجميع الشاشات بفضل تقييد العرض (max-w-5xl).
4. **شريط جانبي تفاعلي مرن (Resizable Admin Sidebar):**
   * تزويد الشريط الجانبي في لوحة تحكم المسؤول بميزة السحب للتكبير والتصغير عبر الماوس (Drag to Resize) وحفظ المقاس المختار تلقائياً في التخزين المحلي (Local Storage).

### ي. تحديث رابط خادم Evolution API (Evolution API URL Update):
* تم تحديث الرابط الأساسي لخادم `Evolution API` في كافة ملفات المشروع (مثل `.env.local` وملفات التوثيق) من `https://evo.magicaikrd.com` إلى `https://evo.mstoviral.online/` لضمان توجيه الاتصالات والمصادقة بشكل صحيح إلى الخادم الجديد.

### ك. تعزيز نظام المبيعات والتسعير في الاشتراكات (Sales & Pricing Enhancements):
1. **الميزات الحصرية للاشتراك السنوي (Yearly-Only Features):**
   * **قاعدة البيانات:** إضافة حقل `yearly_only` إلى جدول `plan_feature_assignments` (عبر Migration `057`).
   * **لوحة التحكم (Admin UI):** إضافة زر تبديل (Toggle) بجانب كل ميزة في إعدادات الباقة، يسمح للمدير بجعل هذه الميزة تظهر وتحتسب فقط للعملاء الذين يدفعون سنوياً. تم إصلاح خلل عرض هذا الزر ليتماشى تماماً مع تصميم المنصة وإزالة صنف التعارض (`justify-center`).
   * **صفحة الهبوط (Landing Page):** برمجة منطق إخفاء هذه الميزات تلقائياً عند قيام الزائر باختيار (دفع شهري)، وإظهارها لتشجيعه عند تبديل الفوترة إلى (دفع سنوي).
2. **ميزة السعر القديم / الخصم (Discount Pricing Strikethrough):**
   * **قاعدة البيانات:** إنشاء حقول جديدة `original_price_monthly` و `original_price_yearly` داخل جدول `subscription_plans` (عبر Migration `058`).
   * **واجهة التسعير (Landing Page):** تعديل بطاقات الأسعار لتعرض السعر القديم (مائل ومشطوب باللون الأحمر وبحجم بارز جداً `2xl` وزاوية 12 درجة) بشكل ديناميكي فوق السعر الحالي لزيادة معدلات التحويل (Conversion Rates).
   * **إصلاح منطق الحفظ للأدمن:** تم اكتشاف وإصلاح خلل برمجي قديم في دالة `handleSavePlan` كان يقوم بالكتابة فوق الأسعار الجديدة ببيانات `billingOptions` المخزنة مسبقاً، حيث تم تعديل المنطق ليعطي الأولوية لمدخلات مدير النظام.

### ل. الأوسمة الديناميكية والدعم الفني الذكي (Dynamic Badges & Support Chat Widget):
1. **نظام أوسمة الخطط الديناميكية (Dynamic Plan Badges):**
   * **قاعدة البيانات:** إضافة عمود `badge_type` لجدول `subscription_plans` لدعم أنواع متعددة من الأوسمة (بدلاً من الاكتفاء بخيار الصح القديم).
   * **لوحة التحكم والتصميم:** تم استبدال خيار (Highlighted Package) بقائمة منسدلة ذكية تتيح اختيار (الأكثر مبيعاً، القيمة الأفضل، عرض محدود، نوصي به). وتم إضافة رسوم متحركة (Animations) وتدرجات لونية (Gradients) مخصصة لكل وسام في صفحة الهبوط لتشجيع العميل على الشراء.
2. **نافذة الدعم الفني المصغرة (MK Whats Support Widget):**
   * **تخصيص الواجهات:** إخفاء أزرار الدعم الفني (WhatsApp & Telegram) برمجياً من جميع صفحات الإدارة (`/admin`) لتوفير مساحة عمل نظيفة للأدمن.
   * **البرمجة التفاعلية:** تحويل زر الواتساب في صفحات الموقع إلى نافذة محادثة منبثقة (Chat Widget) تعمل كمساعد افتراضي. تسأل العميل عن (لغته المفضلة، بريده الإلكتروني، رقم هاتفه، وسؤاله) عبر خطوات متسلسلة (Multi-step Form)، ثم تقوم بصياغة رسالة منسقة وتحويلها لتطبيق الواتساب بشكل تلقائي.

### م. الفحص الأمني وهندسة الحماية (Security Audit & Protection):
* **حماية قواعد البيانات (Supabase RLS):** تم الفحص والتأكد من تطبيق سياسات العزل القوية (`auth.uid() = user_id`) على جميع الجداول الحساسة (رسائل، جهات اتصال، الخ) لضمان عدم إمكانية تسريب بيانات العملاء.
* **حماية الإدارة والمتغيرات (Admin Auth & Envs):** التأكد من أن جميع واجهات الـ (Backend) محصنة تماماً من هجمات تصعيد الصلاحيات (Privilege Escalation) ولا يمكن الوصول لها إلا بدور `super_admin`. كما تم التأكد من إخفاء مفاتيح (Service Role Key) عن المتصفحات.
* **توصية Webhook:** تم تشخيص ثغرة استقبال محتملة في مسار `Evolution Webhook` تسمح باستقبال رسائل وهمية إذا عُرف الرابط، وتم توثيق الحاجة المستقبلية لإضافة نظام (Secret Token Authentication) لحماية الرصيد الآلي.

### ن. إصلاحات صندوق الوارد والمظهر (Inbox & Theme Hotfixes):
* **تخصيص موظفي الحساب (Inbox Assignees Fix):** تم حل مشكلة ظهور أسماء عشوائية (مثل العملاء أو مستخدمي النظام الآخرين) في قائمة التعيين (Assign) داخل صندوق الوارد. تم استبدال الاستعلام المباشر من جدول `profiles` باستخدام واجهة برمجة التطبيقات المخصصة `/api/account/members` لضمان عرض الموظفين الموثقين فقط داخل مساحة العمل الخاصة بالمشترك.
* **تجاوب الرسوم البيانية (Recharts Resize Warning):** إصلاح خطأ `ResponsiveContainer` في واجهة تحكم المشرف (Admin Analytics) بضبط الخصائص `minWidth={0}` و `minHeight={0}` لضمان استجابة وتمدد الرسوم البيانية بشكل سليم دون التسبب بأخطاء في الكونسول (Console).
* **إصلاح وميض المظهر (Theme FOUC Fix):** تم التخلص من رسالة الخطأ المتعلقة بـ `<script>` المزعجة والتي كانت تظهر بسبب تعارض مكتبة `next-themes` في التخطيطات الفرعية (Nested Layouts) الخاصة باللوحة الإدارية. تم استبدال `AdminThemeProvider` باستخدام سياق المظهر العام للمنصة لتوحيد سمة النظام (Dark/Light Mode).

### س. إصلاحات لوحة الإدارة وتجاوب المستخدمين (Admin UI & Responsive Hotfixes):
* **نقطة الفاصلة المنقوطة الغريبة (;):** تم التخلص من مشكلة ظهور علامة (;) في أسفل جميع صفحات الإدارة بسبب خطأ مطبعي في المكون الأساسي `admin-wrapper.tsx`.
* **تجاوب شريط الفلاتر والبحث:** تم تحسين شريط البحث والفلاتر في صفحة إدارة المستخدمين ليلتف بشكل مرن (`flex-wrap`) على شاشات الهواتف المحمولة بدلاً من التداخل والطفح خارج الشاشة.
* **القص الديناميكي للنصوص الطويلة:** إضافة خاصية `truncate` لأسماء الحسابات ورسائل البريد الإلكتروني في الجداول (مثل قائمة "آخر الحسابات المسجلة" وقائمة "المستخدمين") لمنع تشويه الواجهات ودفع العناصر الجانبية (مثل الأوسمة) خارج إطار الشاشة على الجوال.

### ع. ربط المواعيد والتحكم التفاعلي بالذكاء الاصطناعي وبناء خادم Coolify (Appointments & AI Multi-turn Loop & Deployment Fix):
1. **قاعدة البيانات (Appointments Table):** تطبيق ترحيل SQL وإنشاء جدول `appointments` لربط جهة اتصال واتساب (`contact_id`) ومعرّف المحادثة (`conversation_id`) بحدث جوجل كالاندر (`calendar_event_id`) وحفظ تفاصيل المريض الفعلي وتوقيت Baghdad الصارم.
2. **محرك الأتمتة وجوجل كالاندر:** تعديل `createCalendarEvent` في [engine.ts](file:///c:/Users/Mustafa/Desktop/wacrm/src/lib/automations/engine.ts) ليحفظ المواعيد في قاعدة البيانات تلقائياً ويرجع الكائن المحتوي على رابط المعاينة (`htmlLink`). وإضافة دالة `deleteCalendarEvent` للتحكم في الإلغاء.
3. **توطين وتوسيع إشعارات تيليجرام:** تحديث صياغة إشعارات المواعيد بتوقيت بغداد الصارم وتضمين تفاصيل المريض الفعلي ورابط المعاينة المباشر، وتعديل إشعارات Google Sheets لتشمل كافة الأعمدة والحقول المجمعة دون اختصار.
4. **حلقة المعالجة التفاعلية (AI Multi-turn Loop):** برمجة حلقة تفاعلية في [auto-responder.ts](file:///c:/Users/Mustafa/Desktop/wacrm/src/lib/whatsapp/auto-responder.ts) و [engine.ts](file:///c:/Users/Mustafa/Desktop/wacrm/src/lib/automations/engine.ts) لاعتراض تاغات المواعيد `[FIND_MY_APPOINTMENTS]` و `[CANCEL_APPOINTMENT: id]` لتنفيذ أوامر الاستعلام البرمجية والإلغاء خلف الكواليس وتمرير نتائجها للنموذج قبل صياغة الرسالة النهائية للعميل.
5. **معالجة أخطاء التجميع وبناء المشروع (Coolify build fixes):** إصلاح استيراد `deleteCalendarEvent` المفقود في المستجيب التلقائي، وحل تضارب تعريف متغير `busySlotsText` في دالة الأتمتة مما يضمن تجميع وبناء مشروع Next.js ناجحاً بنسبة 100% وخالياً من الأخطاء في خوادم Coolify.

---

## 9. تكاملات ونقاط الربط (Integration Endpoints)
* **جلب معلومات الاشتراك الحالي للعميل:** `GET /api/billing/subscription` (مؤمن بـ JWT ويقرأ بـ Service Role).
* **إدارة وتعديل الخطط يدوياً للأدمن:** `POST /api/admin/users` مع إجراء `update_plan` (مؤمن بصلاحية super_admin).

---

هذا المستند هو دليلك الشامل، ويجب تحديثه باستمرار في حال حدوث تغييرات جوهرية في معمارية النظام أو جداول قاعدة البيانات.


